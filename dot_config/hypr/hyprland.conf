# =============================================================================
# Hyprland Configuration — NOT LOADED
# =============================================================================
# Hyprland's config loader auto-detects ~/.config/hypr/hyprland.lua and uses
# it as THE config, since hyprland.lua doesn't source/require this file back.
# Confirmed via the Hyprland log:
#   [cfg] Regular config at /home/sultonov/.config/hypr/hyprland.lua
#   [cfg] Using lua config found at /home/sultonov/.config/hypr/hyprland.lua
#
# Everything that used to live here (general/decoration/input/dwindle/master/
# cursor/misc extras, window rules, autostart) has been ported into
# hyprland.lua via hl.config()/hl.window_rule()/hl.on("hyprland.start", ...).
#
# This file is kept only as a rollback reference — it has no effect.
# =============================================================================
