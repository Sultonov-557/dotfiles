# Reflector — automatic mirror list management
# Managed by chezmoi

# Fetch the latest 10 mirrors sorted by rate
--latest 10
--sort rate
--protocol https
--country "United States,Germany,Netherlands,France"
--save /etc/pacman.d/mirrorlist
--connection-timeout 5
--download-timeout 10
