# =============================================================================
# packages.txt — Curated list of packages for this system
# One package per line. Lines starting with # are comments.
# Used by: scripts/install-packages.sh
# =============================================================================

# ── Core ──────────────────────────────────────────────────────────────────────
chezmoi
git
base-devel

# ── Shell & Terminal ──────────────────────────────────────────────────────────
fish
ghostty
starship
zoxide
atuin
tmux

# ── Display Server / WM ───────────────────────────────────────────────────────
hyprland
hyprpaper
hyprlock
hypridle
hyprpicker
xdg-desktop-portal-hyprland
polkit-gnome
grimblast-git
noctalia-shell # Wayland shell/bar (cachyos repo); config in dot_config/noctalia

# ── Fonts ─────────────────────────────────────────────────────────────────────
ttf-jetbrains-mono-nerd
ttf-firacode-nerd
noto-fonts-emoji
ttf-font-awesome
noto-fonts
noto-fonts-cjk

# ── Development ───────────────────────────────────────────────────────────────
neovim
lazygit
ripgrep
fd
fzf
bat
eza
tree
jq
yq
direnv
trash-cli
github-cli # provides `gh`; official pkg name differs from the binary
nodejs
npm
python
python-pip
# rustup — run `rustup default stable` after install
rustup
go

# ── Screenshots & Screen Recording ────────────────────────────────────────────
grim
slurp
swappy
# wl-screenrec  # AUR — already listed in the AUR section below
imagemagick
wl-clipboard
cliphist

# ── Audio & Brightness ────────────────────────────────────────────────────────
pipewire
wireplumber
pipewire-audio
pipewire-pulse
playerctl
brightnessctl
helvum
pavucontrol

# ── Network & Bluetooth ───────────────────────────────────────────────────────
networkmanager
network-manager-applet
adguardhome  # DNS-level ad blocker (systemd service, not Docker)
blueman
bluez
bluez-utils
iwd

# ── Notifications ─────────────────────────────────────────────────────────────
dunst
libnotify

# ── Launcher ──────────────────────────────────────────────────────────────────
rofi
rofi-emoji

# ── Apps ──────────────────────────────────────────────────────────────────────
nautilus
obsidian
btop
fastfetch
firefox
zen-browser
mpv
imv
zathura
zathura-pdf-mupdf
file-roller
udiskie
qalculate-gtk
wlogout
obs-studio
yazi
spotify-launcher
# spicetify-cli  # AUR — install via paru once run_once_install-aur-helper.sh has run

# ── System ────────────────────────────────────────────────────────────────────
ufw
tumbler
cups
ipp-usb
print-manager
poppler
gnome-keyring
unrar
p7zip
unzip
lz4

# ── AUR (uncomment per machine) ───────────────────────────────────────────────
bibata-cursor-theme
papirus-icon-theme
herdr-bin # terminal multiplexer, replaces tmux; config in dot_config/herdr
glance-bin # self-hosted dashboard; config in dot_config/glance, reverse-proxied via Caddy
# catppuccin-gtk-theme-mocha
# grimblast-git (if not in official repos)
# wl-screenrec
# rofi-emoji

# Pentesting/recon — were miscategorized as official, "target not found" on install
amass-bin
sherlock # no -bin variant in AUR
subfinder-bin
nuclei-bin
burpsuite # no -bin variant; upstream package just wraps a prebuilt .jar
whatweb # no -bin variant; Ruby script, nothing to build
katana-bin # plain "katana" in AUR is Foundry's VFX tool, NOT this crawler — bin is the only correct pkg
assetfinder # no -bin variant in AUR
dnsx-bin

# From nixul audit (2026-07-28) — AUR-only or unverified-on-Arch packages.
# Uncomment what you actually use; verify exact AUR pkg names on first boot,
# the names below are best-effort guesses based on the upstream project name.
# google-chrome
# niri                  # alt WM, enabled alongside Hyprland on sentinel
# aseprite
# blockbench
# yandex-music
# lutgen
# gogdl                 # GOG downloader used by Heroic/Lutris
# xmcl-bin              # Minecraft launcher (nix used its own xmcl flake input)
# todoist-electron
# worktrunk             # git worktree manager
# harlequin             # SQL TUI client
# usql
# devenv
# bruno-bin             # HTTP client GUI
# ngrok
# zrok
# biome                 # or: npm i -g @biomejs/biome
# lazydocker
# android-studio
# jwt-hack
# waydroid
# tty-clock
# kilocode-cli          # or: npm i -g kilocode
# codex                 # OpenAI Codex CLI — check current install method
# gemini-cli            # Google Gemini CLI
# navi                  # may now be in official repos — check first

# ── From old NixOS config (migrated) ─────────────────────────────────────────
# Gaming
steam
gamescope
gamemode
lutris
wine
wine-gecko
wine-mono

# Disk & System
gparted
baobab
ncdu
mission-center

# Communication (comment out if not wanted)
telegram-desktop
discord
thunderbird

# Network
nmap
wireshark-qt
tcpdump
tailscale
doggo

# CLI tools
htop
procs
thefuck
hyperfine
lnav
television
xh
hurl

# Keyboard firmware
qmk
# via — AUR: via-bin

# Torrent
qbittorrent

# Power management
power-profiles-daemon
thermald
upower

# Printing extras
hplip
sane
simple-scan

# Office
libreoffice-fresh

# Calendar
khal
vdirsyncer

# Disk imaging
# caligula — AUR: caligula-git

# ── Web Servers & Dashboards ────────────────────────────────────────────────
caddy
# glance-bin — AUR, see the bounded AUR section above

# Audio extras
pulsemixer
easyeffects

# ── AI / ML ──────────────────────────────────────────────────────────────────
ollama
ollama-cuda
opencode
# opencode-desktop  # AUR — install via paru once run_once_install-aur-helper.sh has run

# ── Pentesting / Security (from old NixOS config) ────────────────────────────
nmap
wireshark-qt
tcpdump
hydra
john
hashcat
# wordlists — AUR: wordlists (rockyou, etc.)
metasploit
bettercap
ettercap
openbsd-netcat # was "netcat-openbsd" — that's the AUR/upstream project name, Arch's official package is "openbsd-netcat"
inetutils
# caido — AUR: caido-bin
wifite # was "wifite2" — Arch's official "wifite" package is already wifite2 upstream (2:2.8.2)
# websploit — AUR: websploit-git

# ── Databases (from old NixOS config) ───────────────────────────────────────
postgresql
postgresql-libs
redis

# ─── Kubernetes / Containers (from old NixOS config) ────────────────────────
# kubectl
# minikube    — verify: AUR or official, uncertain
# kind        — verify: AUR or official, uncertain

# ── NVIDIA (sentinel + vanguard only — both NVIDIA; skip on archbook/AMD) ────
# packages.txt has no per-host conditionals, so this whole block is commented
# rather than risk installing nvidia on the AMD laptop. Requires [multilib]
# enabled in /etc/pacman.conf first (also needed for Steam/Proton above).
# nvidia
# nvidia-utils
# nvidia-settings
# lib32-nvidia-utils
mesa
# lib32-mesa  — needs multilib, see note above

# CPU microcode — uncomment the one matching this machine
# amd-ucode
# intel-ucode

# ── Wayland/system core additions (from nixul audit) ─────────────────────────
xorg-xwayland
rtkit
zram-generator
openssh

# ── File managers (alternates to nautilus/yazi already above) ───────────────
dolphin
nemo
thunar

# ── Terminals (alternate to ghostty already above) ───────────────────────────
alacritty
foot

# ── Media additions ───────────────────────────────────────────────────────────
ffmpeg
vlc

# ── Companion device ─────────────────────────────────────────────────────────
kdeconnect

# ── Dev: multiplexers, runtimes, git ──────────────────────────────────────────
zellij
yarn
zig
android-tools

# ── Dev: db & http clients ────────────────────────────────────────────────────
# dbeaver     — verify: AUR or official, uncertain

# ── Pentesting / Security additions ───────────────────────────────────────────
fail2ban
certbot   # Arch equivalent of NixOS's security.acme (internal CA / Let's Encrypt)
# unbound     — binds port 53, conflicts with adguardhome unless scoped to
#               127.0.0.1/::1 like nixul does. Uncomment once that's set up.
# sops        — verify: AUR or official, uncertain

# ── Containers / virtualization ───────────────────────────────────────────────
docker
podman
libvirt
# qemu-desktop — verify exact package name; Arch restructured qemu packaging
#                (qemu-base/qemu-desktop/qemu-full/qemu-emulators-full) in 2023
virt-manager

# ── VPN ────────────────────────────────────────────────────────────────────────
openvpn
wireguard-tools

# ── Self-hosted servers ───────────────────────────────────────────────────────
# nginx complements caddy here, not replaces it: nixul's nginx module generates
# an internal CA + wildcard cert and reverse-proxies the service stack below.
# caddy (dot_config/caddy/Caddyfile) stays as the lightweight standalone option.
nginx
# vaultwarden  — verify: AUR or official, uncertain

# ── Optional heavier self-hosted stack (uncomment what you actually run) ─────
# dashy         — nix builds a custom derivation; on Arch, run via Docker instead
# uptime-kuma   — run via Docker instead
# home-assistant, litellm, open-webui, avahi, winboat — vanguard-only (home PC),
#   not enabled on sentinel; home-assistant/litellm/open-webui are typically
#   run via Docker rather than native packages either way
# avahi
# CLI extras
# dua — Arch package is `dua-cli`, not `dua`
dua-cli
pamixer
lsof

# ── Linters/formatters (needed by dot_config/nvim/lua/plugins/{lint,conform}.lua) ──
shellcheck
# hadolint  # AUR (hadolint-bin) — install via paru once run_once_install-aur-helper.sh has run
luacheck
golangci-lint
stylua
vale
yamllint
python-black # nix/pip name is "black"; Arch package is prefixed "python-"
# prettier — not in official repos; install via `npm i -g prettier` (nodejs/npm above)
