# Ripgrep config
# Managed by chezmoi

# Search behavior
--smart-case
--hidden
# Let .gitignore handle exclusions (don't use --no-ignore-vcs)

# Performance
--max-columns=150
--max-columns-preview
--max-count=500
--sort=path

# Only exclude dirs that aren't typically in .gitignore
--glob=!.git/
